import { roboto } from "@/lib/utils";

export default function Home() {
  return (
    <div className={roboto.className && "p-4 min-h-screen grid grid-rows-[20px_1fr_20px]"}>
      home
    </div>
  );
}
